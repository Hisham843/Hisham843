import 'package:flutter/material.dart';

class ValidatorHome extends StatelessWidget {
  const ValidatorHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
